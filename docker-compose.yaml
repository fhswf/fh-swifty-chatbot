services:
  fh-swifty-ui:
    build:
      context: .
      dockerfile: Dockerfile
      platforms:
        - linux/amd64
    image: fh-swifty-ui
    container_name: fh-swifty-ui
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
