[project]
name = "crawler"
version = "0.1.0"
description = "Crawler für die FH-SWF Website"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "scrapy>=2.13.3",
]
